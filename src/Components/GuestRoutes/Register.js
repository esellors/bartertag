import React from 'react';

export default function Register() {
   return (
      <div>
         Guest Registration
         <button>Clear</button>
         <button>Submit</button>
      </div>
   );
}